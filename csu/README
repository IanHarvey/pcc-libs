$Id$

OS-depdendent and target-dependent:

crt0		startup code for programs compiled -static
crt1		startup code for programs compiled -dynamic
gcrt1		profiling startup code for programs compiled -dynamic
crti		beginning of _init() and _fini() functions (inside .init section)
crtn		end of _init() and _fini() functions (inside .fini section)

OS-dependent but target-independent:

crtbegin	support for calling global ctors/dtors
crtend		end of the ctor/dtor lists
